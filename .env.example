# Supabase Configuration (Required)
VITE_SUPABASE_URL=your-supabase-project-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# OAuth Redirect (Optional - defaults to window.location.origin + '/split')
VITE_REDIRECT_URL=https://your-domain.netlify.app/split

# OCR Configuration (Add to Netlify Environment Variables, NOT here)
# Gemini API (Free tier: 1,500 requests/day)
# Get key from: https://makersuite.google.com/app/apikey
# GEMINI_API_KEY=your-gemini-api-key
# Note: API keys should be added to Netlify Dashboard, not .env.local
# See OCR_SETUP.md for detailed instructions
